<?php $productsByCommercants = $this->getProductsByCommercant(); ?>
<?php $tmpCommercantId = 0; ?>
<?php if (count($productsByCommercants) > 0) : ?>
<div class="row apdc_home_selection">
	<div class="page-title col-xs-12">
	    <div class="block-promos text-center">
            <?php foreach ($productsByCommercants as $commercantId => $products) { ?>
			<?php
				$shop_info = Mage::helper('apdc_commercant')->getInfoShop($commercantId);//$this->getInfoShop();
				$delivery_days = $shop_info['delivery_days'];
			?>
	    	<div class="inner-box">
                <div class="title-block"><?php echo $this->getCommercantName($products[0]); ?></div>
			    <div class="col-md-5 text-right delivery-days">
                    <h2>Livraison disponible le</h2>
					<div>
						<table>
							<thead>
								<?php
									foreach($delivery_days as $key => $value) {
										$class = '';
										if($value == 1) {
											$class = 'no';
										}
										else {
											$class = 'yes';
										}
										echo '<th class="'.$class.'">'.$key.'</th>';
									}
								?>
							</thead>
							<tbody>
								<?php
									foreach($delivery_days as $key => $value) {
										if($value == 1) {
											echo '<td><i class="fa fa-close"></i></td>';
										}
										else {
											echo '<td><i class="fa fa-check"></i></td>';
										}
									}
								?>
							</tbody>
						</table>
					</div>
                </div>
				
				<ul class="owl-carousel apdc-products-grid">
					<?php
						foreach ($products as $product) { 
							$this->getChild("card-product")->setData("product", $product);
							echo $this->getChildHtml('card-product', false);
						}
					?>
                </ul>
            </div>
			<?php } ?>
        </div>
    </div>
</div>
<?php endif; ?>
