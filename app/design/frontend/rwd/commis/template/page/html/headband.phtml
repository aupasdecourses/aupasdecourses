<?php

$headband = $this->getLayout()->createBlock('cms/block')->setBlockId('headband')->toHtml();
if ($headband != '') {
	$html = "<div class='page-title'>";
	$html .= $headband;
	$html .= "</div>";
	echo $html;
}
